# Globaleaks - Luna

To build the image locally:

`docker buildx build -t globaleaks /path/to/globaleaks -f /pth/to//globaleaks/docker/Dockerfile.deploy`

The official image will be builded by GitLab CI
